# CortxtOS v2 · M0 实现计划

## 现状（已落盘的设计产物，本轮 grilling 全部完成）

- `H:\hgl_blog\DESIGN.md` **v2.1** — 唯一权威需求（定位/信息架构/模块规格/数据模型/里程碑/non-goals）
- `H:\hgl_blog\CONTEXT.md` — 术语表 + 禁用语纪律（ModelProfile 四角色已定稿：primary/background/vision/embedding）
- `H:\hgl_blog\docs\adr\0001–0004` — 自托管取代本地优先 / Pi SDK 嵌入不 fork / SQLite 派生索引(Vault 真相源) / jieba 中文分词

工作区为空目录（greenfield），无现有代码需要兼容。

## 第一步：落盘工单（9 个文件 → `.scratch/cortxtos-v2/issues/`）

按 tracer-bullet 垂直切片拆 8 张票（M1+ 故意不拆，等 M0 真实痛点）：

| 票 | 内容 | 依赖 |
|----|------|------|
| 01 骨架 | pnpm 单包 TS ESM + zod fail-fast 配置 + SQLite 迁移器(基线全表/WAL) + /healthz + Dockerfile/compose/Caddyfile/deploy.sh | 无 |
| 02 认证 | argon2id + 会话 Cookie + 限速 + .env 种子→DB 为准 + 改密页 + /api/* 鉴权中间件 | 01 |
| 03 Vault | 拖拽上传(md/txt/pdf ≤50MB→真相源目录) + sha256 幂等 + 列表/原子删除 + 在线 md 编辑 | 02 |
| 04 入库 | 状态机(queued/indexing/ready/failed可重试) + PDF 抽文本保留页码 + 标题感知分块(~500tok/15%) + jieba→FTS5 + 嵌入→sqlite-vec + UsageRecord(embed) | 03 |
| 05 检索 | 双路召回(FTS5 top20 + vec top20)→RRF→top6 带 Citation + reindex 全量重建 | 04 |
| 06 对话 | Pi SDK 接入 primary 档 + SSE 流式 + 固定管线注入 + 多会话持久化 + 引用卡片(内置查看器/PDF #page=N) + 12 轮截断 + UsageRecord(chat) | 02·04·05 |
| 07 Briefing | 进程内 cron + jobs/runs + daily-briefing(默认 08:00, background 档) + 简报页 + 运行记录 | 04（可∥05/06） |
| 08 上线 | 深色霓虹主题(视觉模型出稿→CSS 实现) + 实机部署(106.55.102.231: Docker/firewalld/swap/Caddy TLS) + 备份 cron | 01–07 + 用户提供 SSH/API key/DNS |

## 第二步：按 01→08 顺序实现

关键技术点（均已有 ADR/设计背书）：
- **栈**：Node 22 + TS ESM、better-sqlite3(WAL+FTS5)+sqlite-vec、@node-rs/jieba、argon2、Vue 3 + Vite + Tailwind、Pi `@earendil-works/pi-agent-core`/`pi-ai`（lockfile 固定）
- **测试**：vitest 单测覆盖 配置校验/迁移幂等/分块/RRF 融合/SSE 截断/引用构造；每张票完成跑 typecheck + 全量测试再进下一张
- **模拟**：嵌入/LLM 调用做成可 mock 接口，本地无 key 也能跑通全部管线单测；真实模型对话留到 08 现场终验
- 每张票落地后在工单文件勾验收清单，git 逐票提交

## 第三步：M0 验收（= DESIGN §10）

① Compose 一键起 ② 登录后拖拽上传 ③ 带可点击出处问答 ④ 深色霓虹主题 ⑤ briefing 入页面。1–7 我在本地验证，第 8 张需要你届时提供**部署三件套：SSH(root@106.55.102.231)、聚合平台 API key、DNSPod A 记录 cortxt.hgl123.icu→106.55.102.231**。

## 完成后

M0 交付 = 可用的自托管知识库问答 + 每日简报。M1（工具深挖+任务管理页）等你用过 M0 后按真实痛点再立票。