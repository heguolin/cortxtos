# CortxtOS 环境变量
# 服务器: /opt/cortxtos/.env（由 deploy.sh 从本文件生成）
# 本地开发: 仓库根目录 .env（已被 git 忽略）

# 登录密码（首次启动种子入 DB，此后以 DB 为准 —— 见 DESIGN §8）
APP_PASSWORD=change-me-please

# 聚合平台（OpenAI 兼容端点）
LLM_API_KEY=
LLM_BASE_URL=

# 可选覆盖
# CORTEXT_DATA_DIR=/data
# PORT=3000
# CORTEXT_DOMAIN=cortxt.hgl123.icu
