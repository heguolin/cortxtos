# 06 · 对话：固定管线 + SSE 流式 + 引用卡片

**What to build**
- 接入 Pi：`@earendil-works/pi-agent-core` + `pi-ai`（lockfile 固定），`models.json` 由 config 派生；chat 固定走 `primary` 档（ADR 0002）。
- **M0 固定管线**（DESIGN §4.2）：每条消息自动检索 top6 注入，不绕 agent loop。
- `POST/GET /api/sessions`、`POST /api/sessions/:id/messages`（SSE 流式，停止按钮断开即释放）；会话/消息落库（含 citations JSON + usage）。
- 系统提示词：事实性回答必须带编号引用；上下文 = 最近 12 轮 + 本次检索结果。
- 对话 UI：多会话列表、流式渲染、引用卡片（点击 → 内置查看器：md 渲染预览 / PDF 浏览器原生预览跳 `#page=N`）。
- 失败显式报错 + 手动换档提示（M0 只读展示阵容）；`usage_records(purpose='chat')` 强制落库。

**Blocked by** 02 · 04 · 05

**验收清单**
- [ ] 真实 key：对样例库提问，回答正确且每条事实句带引用卡片（需用户提供 key 终验）
- [ ] 点击引用 → 查看器打开且 PDF 落对页
- [ ] 停止按钮 → 流即断、后端释放
- [ ] 无 key/网络失败 → 界面明确报错，不静默
- [ ] usage_records 出现 chat 行，tokens 与供应商后台量级一致

ready-for-agent: ✅（SSE/截断/引用渲染可 mock 单测；真实对话留待 08 现场终验）
